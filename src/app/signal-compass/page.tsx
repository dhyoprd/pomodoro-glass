import { SignalCompassApp } from '@/ui/SignalCompassApp';

export const metadata = {
  title: 'Signal Compass',
  description: 'Macro regime intelligence dashboard.',
};

export default function SignalCompassPage() {
  return <SignalCompassApp />;
}
